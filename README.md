### ***Выполнила Шарипова А.И., гр.ПИ-432Б***
---
# Kubernetes Deployment 

> Документация по развертыванию Kubernetes-деплоя и описанию приложения.


<div style="background: #f9f9f9; padding: 10px; border: 1px solid #ccc;">
  <strong>Примечание</strong> Этот проект предназначен для <i>развертывания контейнеризированного приложения</i> в <b><i>Kubernetes</i></b>  с использованием стандартных манифестов. Деплой обеспечивает автоматическое управление репликами, обновлениями и устойчивостью приложения.
</div>

---

##  Назначение деплоя и приложения
Деплой отвечает за:
- запуск контейнеров приложения;
- управление числом реплик;
- автоматический перезапуск при сбоях;
- распределение нагрузки через Service (если используется);
- удобное масштабирование.

Приложение:
- представляет собой веб‑сервис (или API);
- работает в Docker-контейнере;
- масштабируется в зависимости от нагрузки.

---

##  Параметры деплоя
В таблице представлены основные параметры:

| Параметр       | Описание                     | Значение               |
|----------------|------------------------------|-------------------------|
| name           | Имя Deployment               | example-app            |
| replicas       | Количество реплик            | 3                       |
| image          | Docker-образ приложения      | myregistry/app:latest  |
| containerPort  | Порт контейнера              | 8080                    |
| label          | Метка pod'ов                 | app: example            |
| environment  | Переменные окружения контейнера     | ENV=production                  |
| resources    | Лимиты и запросы ресурсов           | cpu: "200m", memory: "256Mi"    |
| namespace    | Пространство имён Kubernetes        | default                         |

---

##  Пример YAML‑манифеста
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: example-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: example
  template:
    metadata:
      labels:
        app: example
    spec:
      containers:
        - name: example-container
          image: myregistry/app:latest
          ports:
            - containerPort: 8080
```

---

##  Команды для применения и проверки
### Применение манифеста
```bash
kubectl apply -f deployment.yaml
```

### Проверка статуса деплоя
```bash
kubectl get deployments
kubectl get pods -l app=example
```

### Просмотр логов контейнера
```bash
kubectl logs -f <pod-name>
```
---

## 📚 Полезные ссылки
- [Документация Kubernetes](https://kubernetes.io/docs/home/)
- [kubectl Cheat Sheet](https://kubernetes.io/docs/reference/kubectl/cheatsheet/)

---

